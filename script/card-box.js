// card container 
document.getElementById("task-btn-F")
    .addEventListener("click", function () {
        const taskBtn = document.getInnerTextById("task-number");
        if(taskBtn> 0){

        }
    })


// btn number 1 
document.getElementById("task-btn-A")
    .addEventListener("click", function () {
        const NavbarTik = getInnerTextById("tik-mark-btn");
        const taskBtn = getInnerTextById("task-number");
        if (taskBtn  > 0) {
            alert("Button updated succesfully");
            const currentNumber = NavbarTik + 1;
            setInnerTextByIdandValue("tik-mark-btn", currentNumber);
            const currentTask = taskBtn - 1;
            setInnerTextByIdandValue("task-number", currentTask);

            const container = document.getElementById("activity-section");
            const p = document.createElement("p");
            p.innerText = `
            You have Complete The Task Add Dark Mode at 12:48:15 PM
            `
            container.appendChild(p);

        }
    })

// btn number 2 
document.getElementById("task-btn-B")
    .addEventListener("click", function () {
        const NavbarTik = getInnerTextById("tik-mark-btn");
        const taskBtn = getInnerTextById("task-number");
        if (taskBtn  > 0) {
            alert("Button updated succesfully");
            const currentNumber = NavbarTik + 1;
            setInnerTextByIdandValue("tik-mark-btn", currentNumber);
            const currentTask = taskBtn - 1;
            setInnerTextByIdandValue("task-number", currentTask);

            const container = document.getElementById("activity-section");
            const p = document.createElement("p");
            p.innerText = `
            You have Complete The Task Add Dark Mode at 12:48:15 PM
            `
            container.appendChild(p);

        }
    })

// btn number 3 
document.getElementById("task-btn-C")
    .addEventListener("click", function () {
        const NavbarTik = getInnerTextById("tik-mark-btn");
        const taskBtn = getInnerTextById("task-number");
        if (taskBtn  > 0) {
            alert("Button updated succesfully");
            const currentNumber = NavbarTik + 1;
            setInnerTextByIdandValue("tik-mark-btn", currentNumber);
            const currentTask = taskBtn - 1;
            setInnerTextByIdandValue("task-number", currentTask);

            const container = document.getElementById("activity-section");
            const p = document.createElement("p");
            p.innerText = `
            You have Complete The Task Add Dark Mode at 12:48:15 PM
            `
            container.appendChild(p);

        }
    })

// btn number 4 
document.getElementById("task-btn-D")
    .addEventListener("click", function () {
        const NavbarTik = getInnerTextById("tik-mark-btn");
        const taskBtn = getInnerTextById("task-number");
        if (taskBtn  > 0) {
            alert("Button updated succesfully");
            const currentNumber = NavbarTik + 1;
            setInnerTextByIdandValue("tik-mark-btn", currentNumber);
            const currentTask = taskBtn - 1;
            setInnerTextByIdandValue("task-number", currentTask);

            const container = document.getElementById("activity-section");
            const p = document.createElement("p");
            p.innerText = `
            You have Complete The Task Add Dark Mode at 12:48:15 PM
            `
            container.appendChild(p);

        }
    })

// btn number 5 
document.getElementById("task-btn-E")
    .addEventListener("click", function () {
        const NavbarTik = getInnerTextById("tik-mark-btn");
        const taskBtn = getInnerTextById("task-number");
        if (taskBtn > 0) {
            alert("Button updated succesfully");
            const currentNumber = NavbarTik + 1;
            setInnerTextByIdandValue("tik-mark-btn", currentNumber);
            const currentTask = taskBtn - 1;
            setInnerTextByIdandValue("task-number", currentTask);

            const container = document.getElementById("activity-section");
            const p = document.createElement("p");
            p.innerText = `
            You have Complete The Task Add Dark Mode at 12:48:15 PM
            `
            container.appendChild(p);

        }
    })
// btn Number 6 
document.getElementById("task-btn-F")
    .addEventListener("click", function () {
        const NavbarTik = getInnerTextById("tik-mark-btn");
        const taskBtn = getInnerTextById("task-number");
        if (taskBtn > 0 || taskBtn == 0) {
            alert("it's 0 now");
            alert("Button updated successfully");
            const currentNumber = NavbarTik + 1;
            setInnerTextByIdandValue("tik-mark-btn", currentNumber);
            const currentTask = taskBtn - 1;
            setInnerTextByIdandValue("task-number", currentTask);


            const container = document.getElementById("activity-section");
            const p = document.createElement("p");
            p.innerText = `
            You have Complete The Task Add Dark Mode at 12:48:15 PM
            `
            container.appendChild(p);

        }
        
    })